;; (defun export-fbound-symbols (package)
;;   "export symbols with function bindings"
;;   (do-symbols (s package)
;;     (when (fboundp s)
;;       (export s package))))

;; (export-fbound-symbols :tek9)
